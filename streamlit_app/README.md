# Streamlit Dashboard

## Run
From project root:

```bash
streamlit run streamlit_app/app.py
```

## Pages
- **Data Validation**: schema checks (Pandera), missing values, timestamp health, label balance
- **Model Metrics**: distributions + ROC/PR/confusion matrix (when labels exist)
- **Results Explorer**: filter and drill into top anomalies
